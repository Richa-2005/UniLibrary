import React from 'react';

const InventorySection = () => {
  return (
    <div className="card">
      <h2>Manage Inventory</h2>
      <p>Search, update, or remove books currently in your library.</p>
      {/* We will build the local search and update tables here next */}
    </div>
  );
};

export default InventorySection;